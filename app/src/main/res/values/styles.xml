<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="AppBaseTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="auth_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/color_00ffffff</item>
    </style>

    <declare-styleable name="LimitEditTextView">
        <attr name="hint_text" format="string" />
        <attr name="limit_num" format="integer" />
    </declare-styleable>

    <style name="xrich_add_opt">
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">@drawable/orange_click_bg</item>
    </style>

    <style name="xrich_item_opt">
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">#00ffffff</item>
    </style>

    <style name="linearLayout_item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@drawable/list_item_background</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">15dp</item>
    </style>

    <style name="PickerDialog" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_timepicker_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_timepicker_exit</item>
    </style>


    <style name="view_width_match">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:background">@color/color_dddddd</item>
    </style>

</resources>
